services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: menu_planner
      POSTGRES_USER: menu_user
      POSTGRES_PASSWORD: menu_pass
    ports:
      - "5432:5432"
    volumes:
      - menu_planner_data:/var/lib/postgresql/data

volumes:
  menu_planner_data:
